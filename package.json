{
	"name": "quickbooks-api",
	"author": "JackNytely <jacknytely@gmail.com>",
	"license": "MIT",
	"version": "0.0.1",
	"description": "An API for interacting with intuit's QuickBooks API",
	"type": "module",
	"scripts": {
		"build": "cd packages/auth && bun build ./src/app.ts --outdir dist --format esm --declaration",
		"publish:auth": "npm publish --workspace @quickbooks-api/auth"
	},
	"workspaces": [
		"packages/*"
	],
	"devDependencies": {
		"@types/bun": "^1.2.2"
	},
	"peerDependencies": {
		"typescript": "^5.0.0"
	}
}
